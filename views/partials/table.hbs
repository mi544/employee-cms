<table>
    <thead>
        <tr>
            {{#each columns}}
                {{#unless (isId this)}}
                    <th>{{this}}</th>
                {{/unless}}
            {{/each}}
        </tr>
    </thead>
    <tbody>
        {{#each data}}
            <tr>
                {{#each this}}
                    {{#unless (isId @key)}}
                        <td>{{this}}</td>
                    {{/unless}}
                {{/each}}
            </tr>
        {{/each}}
    </tbody>
</table>